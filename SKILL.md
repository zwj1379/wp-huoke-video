---
name: wp-huoke-video
summary: 将任意文案提炼重点后，按选定的模板库模板生成竖屏信息图短视频（1080x1920）；模板5证据型的触发判据是"是否上传图片"——传图即用模板5，没传图不用模板5。
description: 当用户提供一段未排版的文案（如补贴政策、产品卖点、活动规则、知识科普等文字内容），要求制作成类似「2026企业补贴清单」那种竖屏 9:16 信息图短视频时使用。本 skill 维护一个可扩展的模板库：模板1深色科技商务风、模板2星空黄卡键值清单版、模板3星空卡片政策条列版、模板4红色放射补贴强调版、模板5证据型信息图（银行回单凭证区+亮黄攻略区，**触发判据＝用户是否上传图片：传了图就用模板5，没传图不用模板5，不审核图片内容**）；用户可通过「模板N」指定样式，也可以提供对标视频让系统复刻为新模板（主路径），或用对话方式描述需求新增模板。本 skill 负责从文案中提炼重点（主标题+副标题+2~5个重点分组+底部引导，可选 footerNote 黄色强调备注），填充到选定的 Remotion 数据驱动模板，渲染出片并合成 BGM。触发场景包括"把这个文案用模板2做成视频"、"帮我用模板3复刻这个视频"、"把这个文案做成短视频"，以及任何"文字/对标视频→竖屏信息图短视频"的请求。
version: 2.3.2
agent_created: true
tags:
  - video
  - remotion
  - text-to-video
  - 短视频
  - 信息图
  - 竖屏
  - 文案转视频
---

# 文案 → 竖屏信息图短视频

## 触发条件

用户提供文案/对标视频，要求做成竖屏 9:16 信息图短视频：

- 给文案 + 说"用模板1" → 用模板1出片
- 给文案 + 不说模板 → 默认用模板1
- 给对标视频 + 说"做成这个风格/复刻这个视频" → 分析后复刻为新模板或匹配现有模板
- 给文案 + 说"换个风格" → 进入模板选择/新建流程
- **上传了图片 + 要求出片/点名模板5 → 用模板5**（先过下方图片检查）
- **没上传任何图片 + 点名模板5 → 不用模板5**，拦截并建议模板1-4
- **只给一个主题、没给文案**（如"做一条大学生补贴的视频"）→ 若用户知识库/本地资料中有相关内容，先检索提炼口径（金额、条件等与原文逐字一致），把拟用的主标题+要点**报给用户确认后再出片**；资料检索不到就问用户要文案，不得自行编造政策口径

### ⚠️ 模板5 图片前置检查（强制，判据＝有没有图）

模板5「证据型」是模板库中**唯一要求图片素材**的模板——画面主体就是用户上传的那张图。

- **判据只有一条：用户本次请求里有没有上传图片。有图 → 出片；无图 → 拦截。**
- **不审核图片内容**：不判断图片是不是到账回单/收款截图，任何图片都直接塞进证据区（整图等比显示）
- **点名也拦截**：用户明确说"用模板5"，但**本次请求没有上传任何图片 → 不进入渲染，直接提示**：「模板5 需要上传一张图片素材才能使用；当前没有图片，无法使用模板5。」随后建议改用模板1-4，或等用户上传图片后再来
- **金额/摘要**：若能从图片中读出金额、摘要，逐字抄录填入 `evidenceAmount`/`evidenceSummary`；读不出就留空，**不得虚构**（组件对应位置会自动留空处理）
- **占位数据禁令**：组件内 `EvidencePlaceholder` 的示例数据（15000 元、××公司等）**仅限开发期版面预览/内部核对使用**，严禁作为交付出片内容

本 skill 的职责是：**确认模板 → 提炼重点 → 组织排版 → 渲染出片**。

## 模板库说明

模板 ID 见 `<workspace>/make-video-template/src/templates/registry.ts`：

| 模板ID | 名称 | 说明 | 默认 BGM |
|---|---|---|---|
| 1 | 深色科技·信息卡片版 | 深蓝渐变科技背景 + 白字蓝描边浮雕标题（青色扫光）+ 白色信息卡片 2~5 组 + 底部律动弧形引导 | `public/bgm/template1.m4a`（原复刻视频4） |
| 2 | 星空黄卡·键值清单版 | 深蓝星空渐变 + 黄色标题胶囊 + 波浪边米色信息卡 + 黑红键值对 + 底部打字光标 | `public/bgm/template2.m4a`（复刻视频1） |
| 3 | 星空卡片·政策条列版 | 深蓝星云背景 + 白色浮雕主标题 + 6~8 个白色圆角胶囊卡片纵向排列 + 黄色强调备注 | `public/bgm/template3.m4a`（复刻视频2） |
| 4 | 红色放射·补贴强调版 | 红色放射状背景 + 金色碎屑飘落 + 黄底黑边主标题 + 白色信息卡片 + 黑色要点 | `public/bgm/template4.m4a`（复刻视频3） |
| 5 | 证据型·回单说服版 | 浅蓝天幕 + 白卡红字主标题 + 橙副标题(扫光) + 满宽银行回单凭证区(红框/箭头/红章) + 过渡语 + 亮黄攻略区 + 弧形摆动 CTA | `public/bgm/template5.m4a`（抽自对标参考视频） |

> **模板5 是图片依赖型**：触发判据是"用户是否上传图片"（传图即用、无图不用，不审内容，详见「模板5 图片前置检查」）。

### 模板5 图片数量规则（用户 2026-09-06 明确）

- **保底 1 张、最多 4 张**：用户承诺上传数量不会超出 1~4 张区间
- **1 张** → 单图静态展示（等比 contain，无切换动画）
- **2~4 张** → 进入轮换，**平均分配时长**（每张停 `237/n` 帧，如 3 张各 79 帧 / 2.6s），切换动画 15 帧卷角

新模板在此追加；已封版模板不再修改。

## 依赖

- 模板工程通常固定在某一个历史 workspace 的 `make-video-template/` 目录下（`meimouren/make-video-template` 克隆 + 已添加 `src/templates/Template1InfoCard.tsx` / `src/templates/registry.ts` / `src/templateData.ts`）。若当前 `<workspace>/make-video-template/` 不存在，先用 `find /Users/a123/WorkBuddy -maxdepth 3 -name make-video-template -type d` 定位，再进入该目录执行渲染。
- Node 18+、npm 依赖已安装（`npm install` 已跑过）
- FFmpeg 可用
- **内置默认 BGM**：工程 `public/bgm/` 目录下按模板ID存放（`template1.m4a` ~ `template5.m4a`，每个模板一首独立 BGM）。用户未提供 BGM 时，按所选模板自动使用对应默认 BGM（脚本会从 CompositionId 提取模板号 → 找对应 `templateN.m4a`）；用户提供了新 BGM 时优先用用户的

## 工作流总览

```
用户文案/对标视频
        → ① 确认/生成模板（模板1 或 新模板N）
        → ② 提炼文案重点
        → ③ 写入 templateData.ts
        → ④ 注册 Root.tsx
        → ⑤ 验证首帧
        → ⑥ 渲染+合成BGM
        → ⑦ 交付桌面
```

详细命令与代码示例见 `references/workflow-details.md`，
设计系统（颜色/字号/动画参数）见 `references/design-system.md`。

## 快速步骤

### 1. 确认模板

- 用户说"用模板1" → 使用 `Template1InfoCard`
- 用户给对标视频 → 按「新增模板流程」复刻，分配新模板ID，并在 `registry.ts` 注册
- 用户没指定 → 默认模板1
- 用户点名模板5 → **先执行「模板5 图片前置检查」**：本次请求没上传图片就直接提示不可使用，不进入渲染；有图直接用，不审核图的内容

### 2. 提炼文案重点

把用户文案拆成四个部分，遵守以下规则：

- **主标题** `mainTitle`：核心卖点。优先保留用户原句，过长则提炼为 6~12 字口号
- **副标题** `subtitle`：一句话补充说明，可自拟，≤12 字
- **重点分组** `groups[]`：2~5 组，每组 = 一个重点
  - 每组 1~4 条要点；要点可用 `{ name, value }`，value 显示为右侧强调值（金额/年份等）
- **底部引导** `footer`：12~14 字以内；**「评论」必须改写为「平论」**

### 2b. 模板5 文案提炼（证据型专用，数据模型不同）

模板5 使用 `Template5Data`（见 `Template5Evidence.tsx`），**不是**模板1-4 的 `TemplateData`（没有 `groups[]`），填充结构如下：

| 字段 | 规则 |
|---|---|
| `mainTitle` 主标题 | 白卡红字，6~12 字口号 |
| `subtitle` 副标题 | 一行 ≤12 字，带循环扫光 |
| `transitionText` 过渡语 | 一行，超长会自动缩字号；承上启下（钱已到账 → 教你怎么拿） |
| `guideHeading` | 默认「补贴攻略：」，可省略 |
| `guideSteps[]` 攻略 | **做法式短句**：动作 + 条件 + 金额，≤3 条，每条保持单行（如"签见习合同3-12个月 补1500元/月"） |
| `evidenceAmount` / `evidenceSummary` | 可选；仅当能从用户上传的图片中**读出**金额/摘要时填（逐字抄录，不得虚构），读不出就留空 |
| `evidenceImage` 图片 | **必填判据**：用户上传的图片（任意图片，不审内容）。把图片放进工程 `public/`（如 `public/receipt.png`），填文件名即可；证据区整图**等比完整显示**（contain，不裁剪不拉伸），替代占位回单。**用户没传图时不得用占位数据出片，直接拦截**。注：证据区约 1080×458（≈2.36:1），用户图多为竖版截图或超宽横幅（如 2414×618），contain 后上下留白居中属正常，不要为填满而裁剪或拉伸 |
| `evidenceImages` 图片组 | 传 2~4 张图时使用（如 `["a.png","b.png","c.png"]`，均放 `public/`）；证据区轮换且**平均分配时长**：每张停留 `237/n` 帧（2 张各 118 帧、3 张各 79 帧、4 张各 59 帧），切换动画 15 帧（旧图透视翻走、新图卷角飞入落平），固定 👉 贴纸。**数量保底 1 张、上限 4 张**；单图时与 `evidenceImage` 等价，无切换动画。模板5 视频总长已对齐 **237 帧 / 7.9s**（template5.m4a） |
| `footer` CTA | 12~14 字以内（「平论区回复111」）；「评论」→「平论」 |

### 3. 写入模板数据

在 `src/templateData.ts` 新增导出（**不要改 `BLANK_TEMPLATE`**）：

```ts
export const MY_TEMPLATE: TemplateData = {
  mainTitle: '主标题',
  subtitle: '副标题',
  groups: [
    { title: '重点一', note: '备注可选', items: [{ name: '要点A' }, { name: '要点B', value: '3.2万' }] },
    // 2~5 组...
  ],
  footer: '平论区回复111',
};
```

### 4. 注册 Composition

在 `src/Root.tsx` 参照 `InfoTemplateVideo` / `InfoTemplateCover` 注册 Composition + Still，
`durationInFrames` = **BGM秒数 × 30**，`defaultProps={{ data: MY_TEMPLATE }}`。

### 5. 验证首帧

```bash
cd <workspace>/make-video-template
npx remotion still src/index.ts MyVideo out/my-f0.png --frame=0
```

检查所有文字完整、无溢出。若标题截断，调对应 `TemplateNInfoCard.tsx` 的 `titleFont` 字号规则。

**模板5（以及任何新版面/新图片组合）必须多走一步**：把渲出的首帧用 `present_files` 给用户确认（如 `~/Desktop/t5_XX_首帧预览.png`），用户认可后再渲完整视频——证据区塞图效果、攻略折行瑕疵都要在这一步暴露，避免整片重渲。

### 6. 渲染 + 合成 BGM

```bash
# 一键（推荐）· 不传 BGM 参数时按 CompositionId 自动匹配 public/bgm/templateN.m4a
bash ~/.workbuddy/skills/wp-huoke-video/scripts/render_and_mux.sh TemplateNVideo 输出名
# N 取 CompositionId 里的数字 1~5：Template1Video / Template2Video / Template3Video / Template4Video / Template5Video
# 脚本会自动从 CompositionId 提取模板号并匹配对应 BGM

# 用户提供了新 BGM 时显式传入（此时视频时长按新 BGM 对齐）
bash ~/.workbuddy/skills/wp-huoke-video/scripts/render_and_mux.sh TemplateNVideo 输出名 /path/to/新BGM.m4a
```

> ⚠️ **必须用上面的脚本，不要手动 ffmpeg 合成**。remotion 渲出的 mp4 自带一条静音音轨，ffmpeg 不加 `-map 0:v:0 -map 1:a:0` 时会默认选中这条静音轨，导致成片"没有音乐"。若确需手动合成，务必带上这两个 `-map` 参数。

### 7. 交付

```bash
cp out/输出名-final.mp4 ~/Desktop/输出名-新版.mp4
open ~/Desktop/输出名-新版.mp4
```

## 新增模板流程

新模板来源分两种：

### A. 对标视频复刻（主要方式）

1. 用户提供参考视频（如 `~/Desktop/复刻视频4.mp4`）
2. 抽帧分析：每 0.3~0.5s 抽一帧，识别**文字内容、字体颜色、描边/浮雕、背景、卡片形状、动画节奏**
3. 在 `src/templates/` 新建 `TemplateN_XXX.tsx`，逐项还原风格
4. 在 `registry.ts` 注册：`id=N`、`name=...`、`component=TemplateN_XXX`
5. 在 `Root.tsx` 注册该模板对应的 `Composition` + `Still`
6. 用户确认效果后封版；后续文案可直接调用「模板N」

### B. 对话描述需求（轻量方式）

用户用语言描述想要的风格（如"红金喜庆风"、"黑底霓虹风"），确认细节后：
- 新建 `TemplateN_XXX.tsx`
- 注册到 `registry.ts` 和 `Root.tsx`
- 出样片确认，确认后封版

**重要原则**：已封版模板不再修改；风格迭代 = 新建模板N+1。

## 设计系统速览（完整版见 references/design-system.md）

- 背景：深蓝渐变 `#081226→#0D2A55→#0A1E3E` + 科技网格 + 青色发光粒子 + 底部光晕
- 主标题：**白→浅蓝灰字面渐变（文字主体浮雕）+ 深蓝挤出厚度 + 1.5px 蓝描边 + 青色窄带循环扫光**
- 副标题：金色胶囊 `#FFD100`，深蓝字 `#0E53A7`
- 卡片：白底圆角，黄色胶囊组标题，蓝色正文/强调值
- 底部引导：弧形文字 + 循环律动（浮动/脉动/光晕）
- 首帧即完整画面；入场动画 0.33s 快速归位

## 注意事项

- 「评论」→「平论」，政策金额必须与原文一致，不得虚构
- **模板3 数据结构约束**：`Template3InfoCard` 只渲染 `groups[0].items`（单组平铺胶囊卡片），不渲染 group.title；items 须为**单行短句**（`whiteSpace: nowrap`，组件已自动缩字号，字数越多字号越小，保证 ≤18 字都不溢出白框）；推荐每条 ≤14 字（46px 最佳），**硬上限 ≤18 字**；主标题 84px 字号下建议 ≤12 字（约 958px 宽），超长会不可控断行
- 模板4 的 InfoCard 忽略 `value` 字段，金额要写进 `name` 文本里；模板4 不渲染 subtitle
- **模板5（证据型）**：触发判据＝**本次请求有没有上传图片**——有图即用模板5（任意图片，不审内容，`evidenceImage`/`evidenceImages` 字段塞入证据区等比 contain），无图即拦截建议模板1-4；图片数量**保底 1 张、最多 4 张**，1 张静态展示、2~4 张平均分配时长轮换（237/n 帧）；金额/摘要能读出就逐字抄录、读不出留空不虚构；攻略 ≤3 条单行短句；BGM 用模板5 专属 `template5.m4a`（抽自对标参考视频，总长 237 帧/7.9s），副标题动画为扫光非浮动，多图切换为卷角动画
- 扫光保持**窄带**（40%→50%→60%），描边/厚度随字号缩放（历史踩坑，见 workflow-details.md）
- 分组 2~5 最稳；超过 5 组卡片过扁，需合并
- BGM 时长决定 `durationInFrames`（秒数 × 30fps）
- 每次只新增 Composition 与新模板，保留既有模板与历史实例
- 模板文件顶部标注 **【已封版，勿改】**，防止误改成熟模板
